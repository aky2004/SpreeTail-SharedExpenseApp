# Backend — Render.com deployment config
# Render reads this to know how to build and start the service.

# Build command: install deps + compile TypeScript
# Start command: run compiled JS with node
